export interface IUser {
    id: string;
    name: string;
    email: string;
    isAdmin: boolean;
    password: string;
}

export interface IDatabaseUser {
    id: string;
    name: string;
    email: string;
    isAdmin: boolean;
    password: string;
}